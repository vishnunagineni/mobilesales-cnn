name: CNN Example
docker_env:
  image: sequential-cnn
entry_points:
  main:
    parameters:
      batch_size: {type: int ,default: 32}
      epochs: {type: int, default: 10}
    command: "python SequentialCNN.py"
